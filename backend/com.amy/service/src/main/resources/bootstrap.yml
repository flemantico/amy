# HTTP Server
server:
    port: 8082

# Spring Cloud Config Server Repository
spring:
    cloud:
        config:
            server:
                git:
                    uri: https://github.com/flemantico/amy.git
                    search-paths: backend/com.amy/config

                    username: 
                    password: 

# Spring properties in case you use different profiles
#    profiles: default
#    application:
#        name: service

    security:
        user:
            name: root
            password: s3cr3t