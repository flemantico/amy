# HTTP Server
server:
    port: 8082

# Spring Cloud Config Server Repository
spring:
    cloud:
        config:
            server:
                git:
                    uri: https://github.com/flemantico/amy.git
                    search-paths: config

                    username: flemantico
                    password: 19FlemA02

# Spring properties in case you use different profiles
    profiles: default
    application:
        name: service

    security:
        user:
            name: root
            password: s3cr3t