server:
  port: 8000

spring:
  profiles: default
  application:
    name: server_config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/flemantico/amy.git
          username: flemantico
          password: 19FlemA02
          skipSslValidation: true
          search-paths: backend/server_data
        native:
          search-locations: classpath:offline-repository/
  security:
    user:
      name: root
      password: s3cr3t